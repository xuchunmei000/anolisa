manifest_version = 2
anolisa_min_version = "0.1.0"

[capability]
name = "cosh"
description = "Deterministic CLI gateway from Agent to OS"
domain = "tools"
stability = "experimental"

[implementation]
components = ["cosh"]

[requires_env]
os = "linux"
arch = ["x86_64", "aarch64"]

[output]
status_label = "COSH"
summary = "Copilot shell launcher and deterministic CLI gateway"
